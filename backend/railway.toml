# Railway detecta Python vía requirements.txt + Procfile.
# No forzar Docker: el despliegue estable del trial usó el builder Python.

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
