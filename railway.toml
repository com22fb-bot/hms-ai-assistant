# Root Directory in Railway must be EMPTY (blank) — not /backend.
# Branch: main

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# CMD lives in Dockerfile; do not override startCommand (avoids path issues).
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
