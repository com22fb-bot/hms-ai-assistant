# Instalación

```bash
cd /workspaces/hms-ai-assistant
unzip -o hms-ai-assistant-toolkit-v3-governance-2026-07-31.zip
chmod +x tools/checkpoint/*.sh
git status --short
./tools/checkpoint/checkpoint.sh "4.3"
```

Después:

```bash
git status --short
git log -3 --oneline
```
